apiVersion: v1
kind: Service
metadata:
  name: quarkus-mcp-gateway
  namespace: mcp-gateway
spec:
  type: NodePort
  selector:
    app: quarkus-mcp-gateway
  ports:
  - name: http
    port: 8888
    targetPort: 8888
    nodePort: 30888
