name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build-uber-jar:
    runs-on: ubuntu-latest
    name: Build uber-JAR

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Build uber-JAR
        run: mvn package -DskipTests

      - name: Copy artifact
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          cp "target/quarkus-mcp-gateway-${VERSION}.jar" "quarkus-mcp-gateway-${VERSION}.jar"
          echo "ARTIFACT=quarkus-mcp-gateway-${VERSION}.jar" >> $GITHUB_ENV

      - uses: actions/upload-artifact@v4
        with:
          name: uber-jar
          path: ${{ env.ARTIFACT }}

  release:
    needs: build-uber-jar
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - uses: softprops/action-gh-release@v2
        with:
          files: artifacts/*
          generate_release_notes: true
